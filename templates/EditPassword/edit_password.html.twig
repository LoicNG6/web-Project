{% extends 'base2.html.twig' %}
{% block body %}
    <div class="center">
        <h1>Editing {{ app.User.firstName }} {{ app.User.lastName }}'s password</h1>
        {{ form_start(pass) }}
            <table>
                <tr>
                    <td>{{ form_label(pass.password.first) }}</td>
                    <td>{{ form_widget(pass.password.first) }}</td>
                </tr>
                <tr>
                    <td>{{ form_label(pass.password.second) }}</td>
                    <td>{{ form_widget(pass.password.second) }}</td>
                </tr>
                <tr>
                    <td></td>
                    <td> <button type="submit" id="btn">Confirm</button></td>
                </tr>
            </table>
        {{ form_end(pass) }}
    </div>
{% endblock %}