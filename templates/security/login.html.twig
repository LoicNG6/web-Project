<!DOCTYPE>
<html>
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="{{ 'login.css' }}" type="text/css">
    <title>authentication</title>
</head>
<body>

<form method="post">

    <div class="center">
        {% if error %}
            <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}
        {% if app.user %}
            <div class="mb-3">
                You are logged in as {{ app.user.username }}
            </div>
        {% endif %}

        <h1>Login</h1>
        <table>
            <tr>
                <td><label for="inputEmail">Email</label></td>
                <td><input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" required autofocus></td>
            </tr>
            <tr>
                <td><label for="inputPassword">Password</label></td>
                <td><input type="password" name="password" id="inputPassword" class="form-control" required></td>
            </tr>

            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

            <tr>
                <td></td>
                <td><button class="btn btn-lg btn-primary" type="submit">Sign in</button></td>
            </tr>
        </table>
    </div>

</form>

</body>
</html>